# 安全说明

## 支持范围

项目只维护最新发布版本。发现安全问题时，请优先使用代码托管平台提供的私密 Security Advisory，不要在公开 Issue 中粘贴漏洞细节、真实邮箱、令牌、管理员密钥、数据库备份或服务器信息。

报告应包含最小复现、受影响版本、影响范围和建议修复方向。请使用虚构数据复现。

## 威胁模型

本项目假设：

- 操作系统管理员和部署者可信；主机被完全控制后，攻击者可以读取运行时密钥和解密数据。
- `/etc/token-admin.env` 或 Docker `.env` 与数据库备份由部署者分别妥善保护。
- Web 应用只监听回环地址或容器私网，公网流量由受信任的 HTTPS 反向代理接入。
- 反向代理会覆盖而不是透传客户端提交的 `X-Forwarded-Proto` 和 `X-Real-IP`。
- 只有一个 Worker 实例；Web 服务默认也使用单进程，以保持登录限速语义一致。

字段加密主要降低数据库文件、磁盘快照或只读数据库泄露造成的直接暴露风险，不能抵御已控制应用进程或同时取得数据库与加密密钥的攻击者。

## 部署要求

- 公网入口必须使用有效 HTTPS 证书；不要通过公网明文 HTTP 输入管理员密钥。
- 所有环境都必须使用有效的 `ADMIN_KEY_HASH`、`SESSION_SECRET` 和 `ENCRYPTION_KEY`；应用不存在可用的弱默认值。
- 设置明确的 `ALLOWED_HOSTS`，禁止通配符；只信任实际反向代理 IP。
- `/etc/token-admin.env` 或 Docker `.env` 权限保持为 `0600`，数据目录仅允许服务账户访问。
- 不要把管理员原始密钥、Bootstrap、数据库、导入 TXT、日志或证书私钥提交到仓库。
- 定期更新依赖并运行 `pip-audit`；部署前运行测试和静态检查。
- 数据库和环境密钥必须成套备份。丢失 `ENCRYPTION_KEY` 后，加密字段无法恢复。
- 登录尝试会在 Argon2 校验前原子占用失败窗口，进程同时执行的昂贵哈希也有固定上限。
- 受保护写接口在解析 JSON、表单或上传前验证会话与 CSRF，并按路由限制正文大小。
- 账号密文把账号稳定标识和字段名绑定到 AES-GCM 认证上下文，旧格式会在启动时分批升级。

## 已知边界

- 会话是带 HMAC 的无状态 Cookie。退出会删除当前浏览器 Cookie；需要立即使所有会话失效时，应轮换 `SESSION_SECRET` 并重启 Web 服务。
- 当前版本没有自动执行字段加密密钥轮换。轮换必须先实现受控的数据重加密流程。
- 登录限速保存在单个 Web 进程内；多 Web 进程部署前必须改用共享限速存储。
- “90 天”是产品提醒窗口，不应被理解为 Microsoft 对所有租户和令牌类型的永久保证。
- 实际容量受 Token 长度、PostgreSQL 索引、审计保留期、磁盘和外部服务限流影响。
