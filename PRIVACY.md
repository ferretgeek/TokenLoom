# TokenLoom 隐私与公开发布清单

本目录按源码白名单生成，不是对生产目录的完整复制。

## 明确排除

- 生产服务器地址、端口、账号、主机指纹和部署密码；
- 管理员原始密钥、Bootstrap、`.env` 和字段加密密钥；
- 真实邮箱、密码、Client ID、Refresh Token、数据库与备份；
- 导入 TXT、任务临时文件、日志、证书、归档包；
- `.venv`、`data`、`__pycache__`、pytest/ruff 缓存；
- 当前部署专用的 SSH/HTTP/HTTPS 共享端口改造脚本。

测试和文档只使用 `.invalid`、`.example`、回环地址与显式合成占位值；截图和演示不连接任何真实邮箱。

## 数据最小化

旧账号格式仍接受密码字段，以兼容既有 TXT；应用会在解析后丢弃密码，只加密保存 OAuth 刷新和取件实际需要的字段。导入临时文件在完成、取消或失败后删除，孤儿文件由维护任务再次清理。

## 发布前复查

每次发布前至少执行：

1. 检查目录内不存在 `.env`、`data`、数据库、日志、证书或压缩包。
2. 用密钥扫描器检查源码与提交历史，而不只是当前工作树。
3. 搜索非回环 IP、非保留示例邮箱、绝对个人路径和高熵字符串。
4. 运行 `ruff`、`pytest`、`pip-audit` 和 JavaScript 语法检查。
5. 从一个空目录重新安装，确认文档中的命令可以工作。
6. 检查 PNG/ICO 元数据、截图可见文字与 Social preview，不保留用户名、路径、通知或生产地址。
7. 检查 GitHub Issues、PR、Release 文本和资产，以及完整可达历史；真实秘密曾公开时必须轮换并清理历史。

当前目录不包含 Git 历史；如果之后导入到已有仓库，仍需单独扫描完整历史。
